# asdf

options(tab.width=4)
options(prompt="R> ", continue="    ")

local({
	r = getOption("repos")
	r["CRAN"] = "https://cran.rstudio.com/"
	options(repos = r)
})

.First = function(){

	message('\nBon jour!\n')

}
