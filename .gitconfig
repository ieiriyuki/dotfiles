[alias]
    br = branch
    ci = commit
    co = checkout
    df = diff
    loga = log --all --oneline --graph --decorate
    logo = log --oneline --graph --decorate
    ls = ls-files
    rog = reflog
    st = status
[merge]
    ff = true
[pull]
    ff = only
